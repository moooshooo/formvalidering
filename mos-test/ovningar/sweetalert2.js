import swal from "sweetalert2"

const tryMe = document.getElementById("tryMe")
const 

tryMe.onclick = () => {
    
    if
    swal.fire({
    title: "Fan va bra!",
    text: "Du kunde inte hålla dig!",
    icon: "success"
});
}